{
  "name": "BooksOL",
  "private": true,
  "version": "0.0.0",
  "description": "Online reading service",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^0.9.0",
    "pdf2json": "^0.6.2"
  },
  "scripts": {
    "start": "http-server -a localhost -p 8000 -c-1 ./app"
  }
}
